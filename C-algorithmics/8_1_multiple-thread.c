/*
#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
#include <process.h>


void run(void* p) {
	MessageBox(NULL, "1", "1", NULL);

} 

void main() {

	MessageBox(NULL,"1","1", NULL);

	_beginthread(run, 0, NULL);
	_beginthread(run, 0, NULL);
	_beginthread(run, 0, NULL);

	system("pause");
}
*/